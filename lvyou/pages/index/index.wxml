<scroll-view scroll-y="{{true}}" style="height:100vh;">
  <view class="ind_1">
    <picker mode="multiSelector" bindchange="bindMultiPickerChange" bindcolumnchange="bindMultiPickerColumnChange" value="{{multiIndex}}" range="{{multiArray}}">
      <view class="ind_2" bindtap="gocity">{{address}}</view>
    </picker>
    <view class="ind_3">
      <van-icon name="location" size="30rpx" color="#111111" />
    </view>
    <view class="ind_4">
      <van-search value="{{ value }}" shape="round" placeholder="请输入关键字搜索景区/目的地" use-action-slot bind:search="onSearch" readonly bindtap="gosearch"></van-search>
    </view>
  </view>
  <swiper indicator-dots="{{true}}" indicator-active-color="#BE7E50" autoplay="{{true}}" circular="{{true}}" class="ind_11">
    <block wx:for="{{adList}}" wx:key="{{index}}">
      <swiper-item bindtap="goText" data-item="{{item.status}}" data-id="{{item.id}}" data-url="{{item.url}}" data-index="{{item.status}}">
        <image class="ind_10" src="{{item.image}}" class="ind_18"></image>
      </swiper-item>
    </block>
  </swiper>
  <!-- <view class="ind_5" wx:if="{{cateGoryList[0]}}">
    <text class="title">新生家长私享</text>
    <view class="ind_6">
      <view class="ind_7" wx:for="{{cateGoryList}}" wx:key="{{index}}" data-id="{{item.id}}" data-ind="{{index}}" bindtap="golist">
        <image class="ind_8" src="{{item.image}}"></image>
        <text class="ind_9">{{item.name}}</text>
      </view>
    </view>
  </view> -->
  <!-- <view class="hotel-box" wx:for="{{hotel}}" wx:for-index="idx" wx:for-item="items" wx:key="{{idx}}">
    <view class="hotel">
      <text class="title">{{items.name}}专区</text>
      <view class="more" bindtap="goHotel">
        <view>查看更多</view>
        <image src="../../images/hotel.png"></image>
      </view>
    </view>
    <view class="list-hotel">
      <view class="list-item" wx:for="{{items.hotelList}}" wx:key="{{index}}" wx:for-item="item1" data-tit="{{item1.name}}" data-id="{{item1.id}}" bindtap="goDetail">
        <image src="{{item1.cover}}"></image>
        <view class="list-name">{{item1.name}}</view>
        <view class="list-price">
          <text style="font-size: 16px">￥{{item1.price}}</text>起
        </view>
      </view>
    </view>
  </view> -->
  <view>
    <view class="s_2"></view>
    <view class="hotel" style="padding: 20rpx 28rpx;">
      <text class="title">10.1-10.7号三亚网红打卡地</text>
      <view class="more" bindtap="goTourist">
        <view>查看更多</view>
        <image src="../../images/hotel.png"></image>
      </view>
    </view>
    <view class="s_2"></view>
    <view wx:if="{{!list[0]}}" style="text-align:center;font-size:24rpx;">
      <text>还没有精选项目，敬请期待</text>
    </view>
    <view style="padding: 0 20rpx;box-sizing:border-box;">
      <list wx:if="{{list[0]}}" list="{{list}}" bindclick="gotoList"></list>
    </view>
    <view class="load" wx:if="{{loading}}">加载中...</view>
    <view class="bottom">
      <image src="../../images/bottom.png"></image>
    </view>
  </view>
  <!-- <view class="ind_12" hidden="{{showBox}}">
    <view class="ind_13">
      <view class="ind_14">
        <text class="ind_15">尊敬的用户：</text>
        <text class="ind_16">
          您好，欢迎您使用环渡旅行旗下的产品！在您试用期间，您有两次无押金下单的使用次数，在您使用期间，有任何问题都可以联系环渡客服，环渡客户7*24小时守护您的出行，愿我们的服务能改变您的生活！
        </text>
      </view>
      <image class="ind_17" src="../../images/i7.png" mode="widthFix" bindtap="hiddenBox"></image>
    </view>
  </view> -->
  <!-- <view class="bgs" wx:if="{{notice == 0}}">
    <view class="imgs">
      <image src="https://oss.huandugj.com/20190827170546c0rQ.png"></image>
    </view>
    <image bindtap="goNotice" class="close" src="../../images/clo.png"></image>
  </view> -->
</scroll-view>